import React from 'react';

export function RepActivityLeaderboard({
  rows,
  isLoading,
  period,
  onPeriodChange,
}: {
  rows: any[];
  isLoading: boolean;
  period: string;
  onPeriodChange: (p: any) => void;
}) {
  return <div />;
}
