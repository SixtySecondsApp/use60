import React from 'react';

export function ReplyIntentBreakdown({
  buckets,
  activeCategory,
  onCategoryClick,
}: {
  buckets: any[];
  activeCategory: string | null;
  onCategoryClick: (c: string | null) => void;
}) {
  return <div />;
}
