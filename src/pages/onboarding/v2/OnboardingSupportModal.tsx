export function OnboardingSupportModal({ open, onClose, orgId, context }: {
  open: boolean;
  onClose: () => void;
  orgId?: string;
  context?: string;
}) {
  if (!open) return null;
  return null;
}
